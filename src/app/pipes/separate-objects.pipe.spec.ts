import { SeparateObjectsPipe } from './separate-objects.pipe';

describe('SeparateObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
